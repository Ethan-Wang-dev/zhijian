{
  "name": "zhijian-extension",
  "version": "0.3.1",
  "private": true,
  "description": "值见：每半小时，找到真正值得看的内容。",
  "scripts": {
    "check": "node --check background.js && node --check content.js && node --check options.js && node --check popup.js && node --check dashboard.js",
    "test": "node --test tests/*.test.cjs"
  },
  "license": "MIT"
}
